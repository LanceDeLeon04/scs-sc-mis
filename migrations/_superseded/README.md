# Superseded

These 3 files are kept only for history. Everything in them (avatars bucket,
member IDs, bootstrap admins) is now included directly in the top-level
`schema.sql`, which is the single file you run on a fresh Supabase project.

Do not run these separately — `schema.sql` already does everything they did,
in the correct order, in one shot.
